# ==============================================================================
# Simple Zero-Config Deploy Workflow
# Place this file in your project repository at: .github/workflows/deploy.yml
# ==============================================================================
name: Deploy to VPS

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: isavage/deploy/.github/workflows/deploy.yml@main
    secrets: inherit

